# =============================================================================
# Core Constants Module
# =============================================================================

add_library(cad_core_constants INTERFACE)

target_include_directories(cad_core_constants INTERFACE
    ${CMAKE_CURRENT_SOURCE_DIR}
)
